[
  {
    "name": "/admin",
    "steps": [
      {
        "expect": [
          {
            "method": "sendMessage",
            "payload": {
              "text": "Access private admin lead list (only for configured admin)"
            }
          }
        ],
        "send": {
          "text": "/admin"
        }
      }
    ]
  }
]
